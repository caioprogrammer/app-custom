import React, { ReactChildren } from 'react';
interface AvaibilityPopupProps {
    children: ReactChildren;
}
export declare const AvaibilityPopup: ({ children }: AvaibilityPopupProps) => React.JSX.Element;
export {};
