import React from 'react';
interface CategoryPaginationProps {
    maxPagesVisible?: number;
}
export declare const CategoryPagination: ({ maxPagesVisible, }: CategoryPaginationProps) => React.JSX.Element;
export {};
