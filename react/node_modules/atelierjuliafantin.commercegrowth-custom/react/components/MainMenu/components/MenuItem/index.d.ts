import React from 'react';
import { CustomLinks } from '../../typings/MainMenuTypes';
export declare const MenuItem: ({ name, href, order, banners, children, hasChildren, setOpenMenu, }: CustomLinks) => React.JSX.Element;
