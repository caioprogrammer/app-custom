export declare type newsletterData = {
    name: string;
    email: string;
    cel: string;
};
export declare const newsletterFactory: () => {
    send: (data: newsletterData) => Promise<unknown>;
};
