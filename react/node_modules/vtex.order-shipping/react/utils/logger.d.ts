export interface LogParams {
    type: 'Error';
    level: 'Critical';
    event: unknown;
    workflowType?: 'OrderShipping';
    workflowInstance: string;
}
export declare type LogFn = (params: LogParams) => void;
export declare type UseLogger = () => {
    log: LogFn;
};
export declare function useLogger(): {
    log: ({ type, level, event, workflowType, workflowInstance, }: LogParams) => void;
};
