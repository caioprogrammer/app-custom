interface Props extends EnhancedIconProps {
    readonly network: string;
    readonly background: string;
}
declare const IconSocial: {
    ({ network, size, background, shape, ...props }: Props): JSX.Element;
    defautProps: {
        size: number;
        viewBox: string;
    };
};
export default IconSocial;
