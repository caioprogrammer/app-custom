declare const Icon: {
    ({ id, handle, isActive, size, viewBox, activeClassName, mutedClassName, }: IconProps): JSX.Element;
    defaultProps: {
        isActive: boolean;
        size: number;
        viewBox: string;
    };
};
export default Icon;
